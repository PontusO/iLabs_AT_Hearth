SB_CONFIG_BOOTLOADER_MCUBOOT=y
SB_CONFIG_MCUBOOT_MODE_SINGLE_APP=y
SB_CONFIG_BOOT_SIGNATURE_TYPE_ECDSA_P256=y
SB_CONFIG_BOOT_SIGNATURE_KEY_FILE="${APP_DIR}/keys/hearth_dev_p256.pem"

# Sysbuild gates CONFIG_CHIP on this: nrf/sysbuild/CMakeLists.txt forces
# CONFIG_CHIP=n on the app image unless SB_CONFIG_MATTER=y here, no matter
# what prj.conf says. SB_CONFIG_MATTER_OTA defaults to y and would force
# CONFIG_CHIP_OTA_REQUESTOR back on the same way, against the prj.conf
# intent (host-driven serial update, not Matter OTA); disable it too.
SB_CONFIG_MATTER=y
SB_CONFIG_MATTER_OTA=n
